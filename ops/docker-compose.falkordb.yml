version: '3.8'

services:
  falkordb:
    image: falkordb/falkordb:latest
    container_name: asta_falkordb
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - falkordb_data:/data

volumes:
  falkordb_data:
